-- Demo seed (run only in dev). Skipped in prod.
-- Requires at least one user to exist in auth.users.

-- (Intentionally empty in this skeleton — the app seeds a starter project
-- the first time a new user signs in.)
